Priority: high/medium/low
Complexity/effort: C1/C2/C4/C8 (weeks)

Zephyr:

- Complete TCP data unhappy paths (C2, high)
- Change the encoding of TCP sequence numbers in test protocol to string (C1, high)
- Integrate with subsys/net/ip/connection.c (C2, high)
- Add the support for IPv6 (C2, medium)
- Drop TCP_NONE state? (<C1, medium)
- Drop tcp_conn_kind? (<C1, medium)
- Sanitize tp_input() (allow only JSON) (<C1, low)
- Drop the first version of the test protocol (C1, low)
- Make tp to register callback in order to be reusable (<C1, low)

TTCN-3:

- Add support for IPv4 options (<C1, medium)
- Include test with TCP options (<C1, medium)
- Document _listen and _connect (<C1, medium)
- Implement RTT discovery (C1, low)
- Include and document user scripts (<C1, low)
